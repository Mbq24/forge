version: '3'
services:
  flask-app:
    build:
      context: ./tradingview/app
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - csharp-model

  csharp-model:
    build:
      context: ./RegressionModelLong
      dockerfile: Dockerfile
    ports:
      - "5001:80"